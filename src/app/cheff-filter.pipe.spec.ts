import { CheffFilterPipe } from './cheff-filter.pipe';

describe('CheffFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CheffFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
